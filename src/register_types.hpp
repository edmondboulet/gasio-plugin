#pragma once

#include <godot_cpp/core/class_db.hpp>

void initialize_gasio_module(godot::ModuleInitializationLevel p_level);
void uninitialize_gasio_module(godot::ModuleInitializationLevel p_level);
